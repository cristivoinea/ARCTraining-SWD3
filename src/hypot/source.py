# Requirements

def sqrt(a: float) -> float:
    """Docstring
    Args:
    a:
    b:
    
    Returns:
    c:"""
    return a**(0.5)

def hypot(a: float, b: float) -> float:
    return sqrt(a**2 + b**2)